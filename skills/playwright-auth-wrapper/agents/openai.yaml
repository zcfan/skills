interface:
  display_name: "Playwright Auth Wrapper"
  short_description: "Launch authenticated playwright-cli sessions for Agents."
  default_prompt: "Use $playwright-auth-wrapper to launch an isolated playwright-cli session with my shared manual login state, then use the official playwright-cli Skill to continue the Agent task in that session."
